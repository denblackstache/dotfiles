[credential]
	helper = osxkeychain
[core]
	editor = vim 
	autocrlf = input
	excludesfile = ~/.gitignore_global
[include]
      path = ~/.gitconfig_local
[pull]
	ff = only
# [merge]
#     tool = idea
# [mergetool "idea"]
#     cmd = idea1 merge $(cd $(dirname "$LOCAL") && pwd)/$(basename "$LOCAL") $(cd $(dirname "$REMOTE") && pwd)/$(basename "$REMOTE") $(cd $(dirname "$BASE") && pwd)/$(basename "$BASE") $(cd $(dirname "$MERGED") && pwd)/$(basename "$MERGED")
#     trustExitCode = true
# [diff]
#     tool = idea
# [difftool "idea"]
#     cmd = idea1 diff $(cd $(dirname "$LOCAL") && pwd)/$(basename "$LOCAL") $(cd $(dirname "$REMOTE") && pwd)/$(basename "$REMOTE")
